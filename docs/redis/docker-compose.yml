services:
  # The Event Bus
  redis-bus:
    image: redis:7.2-alpine
    container_name: rag_event_bus
    # --appendonly yes ensures every message is saved to disk immediately
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - ./redis_data:/data
    restart: always